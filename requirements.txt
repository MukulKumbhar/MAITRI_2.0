# MAITRI 2.0 — Python dependencies
# ⚠️  REQUIRES Python 3.11 (mediapipe does not support Python 3.13+)
#
# RECOMMENDED SETUP (uses Anaconda — fastest, no resolver issues):
#   conda create -n maitri python=3.11 -y
#   conda activate maitri
#   conda install -c conda-forge numpy pandas streamlit opencv -y
#   pip install --prefer-binary deepface mediapipe streamlit-webrtc aiortc
#
# Run the app:
#   conda activate maitri
#   streamlit run app.py
#
# OR via full path without activating:
#   /home/mikey/anaconda3/envs/maitri/bin/python -m streamlit run app.py
#
# ── Conda (batch 1 — fast) ─────────────────────────────────────────────
# conda install -c conda-forge:
numpy>=2.0.0
pandas>=2.0.0
streamlit>=1.35.0
opencv-python-headless>=4.8.0,<5.0.0   # pinned <5.0: OpenCV 5 removed Haar cascade XMLs required by DeepFace

# ── pip (batch 2 — after conda) ────────────────────────────────────────
hsemotion-onnx>=0.3.1                   # Pretrained EfficientNet FER (ONNX Runtime, ~10ms CPU)
onnxruntime>=1.18.0
deepface>=0.0.93                        # Fallback backend
tf-keras>=2.16.0                        # required: DeepFace VGG-Face backend on TF 2.16+
mediapipe>=0.10.9,<0.11.0               # pinned: requires Python < 3.13
streamlit-webrtc>=0.47.0
aiortc>=1.8.0


# ── Voice module (future — uncomment when ready) ───────────────────────
# speechbrain>=1.0.0
# sounddevice>=0.4.6
# soundfile>=0.12.1
# librosa>=0.10.1

